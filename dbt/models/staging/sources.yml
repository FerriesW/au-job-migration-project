version: 2

sources:
  - name: raw
    database: "{{ env_var('GCP_PROJECT_ID', 'au-jobs-radar') }}"
    schema: raw
    tables:
      - name: adzuna_jobs
        description: "Adzuna API job postings, one row per posting per snapshot."
      - name: occupation_lists
        description: "Department of Home Affairs MLTSSL / STSOL / CSOL occupation lists."
      - name: eoi_invitations
        description: "Department of Home Affairs EOI invitation rounds."
